<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-collapsed">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <span class="navbar-brand">Daily Tee Server</span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="header-collapsed">
      <ul class="nav navbar-nav">
          <li class="<%= current_page?(controller: 'sites', action: 'index') ? "active" : "" %>">
            <%= link_to 'Sites', sites_path %>
          </li>  
          <li class="<%= current_page?(controller: 'shirts', action: 'index') ? "active" : "" %>">
            <%= link_to 'Shirts', shirts_path %>
          </li>  
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= current_page?(controller: 'admin', action: 'index') ? "active" : "" %>">
          <%= link_to_if(isAdmin?, "Logout", admin_logout_path) do
                  link_to("Login", admin_index_path) end %>
        </li>  
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>